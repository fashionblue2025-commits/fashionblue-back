# ==============================================
# PRODUCTION ENVIRONMENT
# ==============================================
# ⚠️ IMPORTANTE: Este archivo NO debe estar en el repositorio
# Copia este archivo a .env.production y configura con valores reales

# Application
APP_NAME=fashion-blue
APP_ENV=production
APP_PORT=8080
APP_HOST=0.0.0.0

# Database
# ⚠️ CAMBIAR ESTOS VALORES EN PRODUCCIÓN
DB_HOST=localhost
DB_PORT=5432
DB_USER=CHANGE_ME
DB_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD
DB_NAME=fashionblue_prod_db
DB_SSLMODE=require

# JWT
# ⚠️ GENERAR UNA CLAVE SEGURA ÚNICA
# Ejemplo: openssl rand -base64 64
JWT_SECRET=CHANGE_ME_TO_RANDOM_SECURE_KEY_AT_LEAST_32_CHARS
JWT_EXPIRATION=24h

# File Upload
MAX_UPLOAD_SIZE=10485760
UPLOAD_PATH=/var/app/uploads

# CORS
# ⚠️ Especificar solo los dominios permitidos
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Logging
LOG_LEVEL=info
LOG_FORMAT=json

# pgAdmin
# ⚠️ CAMBIAR CREDENCIALES
PGADMIN_EMAIL=admin@yourdomain.com
PGADMIN_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD
PGADMIN_PORT=5050

# ==============================================
# NOTAS DE SEGURIDAD
# ==============================================
# 1. Nunca subas este archivo con valores reales al repositorio
# 2. Usa contraseñas fuertes (mínimo 16 caracteres)
# 3. Genera JWT_SECRET con: openssl rand -base64 64
# 4. Habilita SSL para la base de datos (DB_SSLMODE=require)
# 5. Restringe CORS solo a tus dominios
# 6. Usa un servicio de secrets management (AWS Secrets Manager, HashiCorp Vault, etc.)
# 7. Rota las credenciales regularmente
# 8. Monitorea los logs de acceso
